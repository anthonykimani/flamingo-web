export enum GameState {
  CREATED = "created",
  WAITING = "waiting",
  IN_PROGRESS = "in_progress",
  COUNTDOWN = "countdown",
  TIMEOUT = "question_timeout",
  RESULTS_READY = "results_ready",
  PAYOUT = "payout",
  COMPLETED = "completed",
}