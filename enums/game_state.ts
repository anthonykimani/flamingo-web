export enum GameState {
  CREATED = "created",
  WAITING = "waiting",
  COUNTDOWN = "countdown",
  IN_PROGRESS = "in_progress",
  RESULTS_READY = "results_ready",
  PAYOUT = "payout",
  COMPLETED = "completed",
}